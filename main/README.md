# File Structure

| File | Relevance | Dependencies | Used by |
| --- | --- | --- | --- |
| firmware_pin_contract.h | Validates the complete fixed V6 Teensy pin map, RTC-I2C exclusions, telescope PWM capability, and digital-pin uniqueness even while feature gates remain disabled. | firmware_config.h, Teensy 4.1 pin contract | main.ino |
| firmware_build_identity.h | Fail-closed identity used by direct IDE builds; the pinned build script replaces it only in its temporary staged sketch with the exact source SHA-256. | None | main.ino, scripts/teensy/build_firmware.sh |
| main.ino | Teensy 4.1 entrypoint for reference-qualified sensor timing, boot-time shutdown of persistent DS3231 SQW state when timing is disabled, bounded inverted status-V VLP NMEA behind per-unit qualifier gates, source-bound build diagnostics, observed camera/IMU event stamps, ROS serial, and fail-closed telescope operation including the D31 field-invalid interrupt. | Arduino, rosserial, RTClib, firmware_config.h, firmware_build_identity.h, firmware_pin_contract.h, sensor_sync_runtime.h, telescope_runtime.h | Firmware build and flash |
| sensor_sync.h | Side-effect-free reference qualification, continuous rollover-safe relative-epoch mapping, trigger scheduling, feedback tracking, and bounded ISR-mailbox primitives. | stdint.h | sensor_sync_runtime.h, main.ino, ephemeral host validation |
| sensor_sync_runtime.h | Single-owner Teensy timer adapter for FIELD_VALID-gated post-one-shot PPS capture, four Forge trigger/ExposureActive channels, one MTi event-marker pair, and timing diagnostics; VLP PPS fanout is hardware-owned. | Arduino, firmware_config.h, sensor_sync.h | main.ino |
| telescope_control.h | Pure calibrated encoder/length conversion helpers that reject zero-valued uncommissioned calibration. | stdint.h, firmware_config.h | telescope_runtime.h, ephemeral host validation |
| telescope_runtime.h | Telescope hardware/state adapter with an independently tested hard-E-stop commissioning gate, immediate NC minimum trip, debounced healthy release, active-high field-valid supervision, falling-edge enable removal, request-driven homing, timeout/stall stops, and break-before-make motor reversal. | Arduino.h, math.h, telescope_control.h, firmware_config.h | main.ino |

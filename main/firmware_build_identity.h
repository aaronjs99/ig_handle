#pragma once

// The reproducible build script replaces this header only in its temporary
// staged sketch. A direct IDE build therefore identifies itself as
// uncommissioned instead of impersonating a reviewed artifact.
namespace ig_handle_firmware_config {
static const char kFirmwareBuildId[] = "uncommissioned-direct-ide-build";
}
